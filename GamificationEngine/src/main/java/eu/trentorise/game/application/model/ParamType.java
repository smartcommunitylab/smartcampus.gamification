package eu.trentorise.game.application.model;

/**
 *
 * @author Luca Piras
 */
public enum ParamType {
    //TODO: evaluate if create a hierarchy instead of an enum, and evaluate
    //further if in that case coulb be useful to tie to every element of the
    //hierarchy a tool able to manage that particular type of param
    //TODO: at the moment are supported only the following type, add the other
    //ones
    INTEGER;
}